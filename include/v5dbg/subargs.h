#pragma once
#include <vector>
#include <string>

/**
 * Parse subarguments from the input string
 * @param in Input string
 * @return List of parameters from `in`
 */
std::vector<std::string> V5Dbg_ParseSubargs(const std::string &in);