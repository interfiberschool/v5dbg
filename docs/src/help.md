# Help page

```
v5dbg - Local debugger client

Commands:
  connect-serial - Connect to the debug server over serial USB
  connect-wireless - Connect to the debug server over a serial->wireless connection
  devices - List all serial devices connected to this machine
```
